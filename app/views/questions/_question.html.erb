<% questions_block_id = "question_#{question.id}" %>

<div id="<%= questions_block_id %>" class="question">
  <div class="question-content">
    <div class="question-text text-lead">
      <%= question.text %>
    </div>

    <% if question.answer.present? %>
      <div class="question-answer text-lead">
        <%= question.answer %>
      </div>
    <% end %>

    <div class="question-additional-information">
      <div class="question-likes">
        <% pre_like = question.likes.find { |like| like.user_id == current_user.id} if current_user.present? %>

        <% if pre_like %>
          <%= link_to content_tag('span', '', class: "fa fa-heart"), question_like_path(question, pre_like), method: :delete %>
        <% else %>
          <%= link_to content_tag('span', '', class: "fa fa-heart-o"), question_likes_path(question), method: :post %>
        <% end %>

        <i><%= question.likes.count %> <%= (question.likes.count) == 1 ? 'Like' : 'Likes'%></i>
      </div>
      <div class="question-date-author-content">
        <div class="question-author">
          <% if question.author.present? %>
            <%= link_to_unless_current "@#{question.author.username}", user_path(question.author) %>
          <% else %>
            <i>Гость сайта</i>
          <% end %>
        </div>
        <div class="question-date">
          <%= l(question.created_at, format: :short) %>
        </div>
      </div>
      <div class="question-link">
        <div class="tooltip">
          <button class="helo">
            <span class="tooltiptext" id="myTooltip">Copy to clipboard</span>
            <%= fa_icon('external-link') %>
          </button>
        </div>
        <%= link_to "##{question.id}", "#{user_path(question.user)}#question_#{question.id}", id: 'myInput' %>
      </div>
    </div>
  </div>

  <% if question.user == current_user %>
    <% edit_icon = question.answer.blank? ? 'reply' : 'pencil' %>

    <div class="question-actions">
      <%= link_to fa_icon(edit_icon), edit_question_path(question) %>
      <%= link_to fa_icon('trash-o'), question_path(question),
                  method: :delete, data: { confirm: 'Точно удалить вопрос?' } %>
    </div>
  <% end %>
</div>
